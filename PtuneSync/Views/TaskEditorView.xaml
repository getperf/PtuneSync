<UserControl
    x:Class="PtuneSync.Views.TaskEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:PtuneSync.ViewModels"
    xmlns:models="using:PtuneSync.Models"
    KeyDown="OnViewKeyDown">

    <UserControl.DataContext>
        <vm:TaskEditorViewModel />
    </UserControl.DataContext>

    <Grid Padding="24">
        <StackPanel Spacing="16">

            <TextBlock Text="今日のタスク"
                       FontSize="20"
                       FontWeight="SemiBold" />

            <ListView x:Name="TaskListView"
                      ItemsSource="{Binding Tasks}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:TaskItem">

                        <Grid Margin="0,4,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="70" />
                                <ColumnDefinition Width="50" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!-- 削除ボタン -->
                            <Button Grid.Row="0"
                                    Grid.Column="0"
                                    Content="✕"
                                    HorizontalAlignment="Center"
                                    Click="OnDeleteTaskClicked" />

                            <TextBox Grid.Row="0"
                                    Grid.Column="1"
                                    Margin="{x:Bind Indent, Mode=OneWay}"
                                    Text="{x:Bind Title, Mode=TwoWay}"
                                    Foreground="{x:Bind TitleForeground, Mode=OneWay}"
                                    Height="36"
                                    HorizontalAlignment="Stretch"/>

                            <!-- 🍅インクリメント -->
                            <Button Grid.Row="0"
                                    Grid.Column="2"
                                    Content="{x:Bind PomodoroLabel, Mode=OneWay}"
                                    HorizontalAlignment="Center"
                                    Click="OnIncrementPomodoroClicked" />

                            <!-- 親/子切替 -->
                            <Button Grid.Row="0"
                                    Grid.Column="3"
                                    Content="⇄"
                                    HorizontalAlignment="Right"
                                    Click="OnToggleHierarchyClicked" />

                            <Grid Grid.Row="1"
                                  Grid.Column="1"
                                  Grid.ColumnSpan="3"
                                  Margin="{x:Bind Indent, Mode=OneWay}"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <ItemsControl Grid.Column="0"
                                              ItemsSource="{x:Bind TagSuggestions, Mode=OneWay}"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" Spacing="6" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="models:TagSuggestionItem">
                                            <ToggleButton Content="{x:Bind DisplayName, Mode=OneWay}"
                                                          Margin="0,0,0,4"
                                                          IsChecked="{x:Bind IsSelected, Mode=TwoWay}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <Button Grid.Column="1"
                                        Margin="12,0,0,0"
                                        Content="{x:Bind GoalLabel, Mode=OneWay}"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Click="OnCycleGoalClicked" />
                            </Grid>

                        </Grid>

                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- ★ 追加したボタン -->
            <Button Content="＋ タスク追加"
                    HorizontalAlignment="Left"
                    Click="OnAddTaskClicked" />
        </StackPanel>
    </Grid>
</UserControl>
