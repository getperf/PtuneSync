<UserControl
    x:Class="PtuneSync.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:PtuneSync.Views">

    <Grid x:Name="RootGrid"
          Background="{ThemeResource SystemControlAcrylicWindowMediumHighBrush}"
          Padding="12">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header + Commands 横一列 -->
            <RowDefinition Height="*"/>
            <!-- TaskEditor (scrollable) -->
            <RowDefinition Height="Auto"/>
            <!-- Status bar fixed -->
        </Grid.RowDefinitions>

        <!-- ★ Header + Commands を 1 行に統合 -->
        <Grid Grid.Row="0"
              ColumnSpacing="12"
              VerticalAlignment="Center">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="PtuneSync"
                       FontSize="20"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>

            <StackPanel Grid.Column="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Spacing="8"
                        VerticalAlignment="Center">

                <Button Content="Pull"
                        Width="100"
                        Height="36"
                        Command="{Binding PullCommand}"/>

                <Button Content="Push"
                        Width="100"
                        Height="36"
                        Background="{Binding PushButtonBrush}"
                        Foreground="White"
                        Command="{Binding PushCommand}"/>

                <Button Content="リセット"
                        Width="120"
                        Height="36"
                        Command="{Binding ResetCommand}"/>
            </StackPanel>

            <TextBlock Grid.Column="3"
                       Text="{Binding SyncModeInlineText}"
                       Foreground="{Binding SyncModeBrush}"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Margin="4,0,4,0"/>

            <Button Grid.Column="4"
                    Content="⋯"
                    Width="36"
                    Height="36"
                    x:Name="SettingsButton"
                    VerticalAlignment="Center"/>
        </Grid>

        <!-- Scrollable TaskEditor -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Padding="0,8,0,8"
                      IsTabStop="False">

            <views:TaskEditorView  x:Name="TaskEditorHost"
                      Grid.Row="3" />
        </ScrollViewer>

        <!-- ★ Status bar：フッター固定 -->
        <Border Grid.Row="2"
                Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
                Padding="8"
                CornerRadius="4"
                Margin="0,4,0,0">

            <TextBlock Text="{Binding StatusMessage}"
                       FontSize="13"/>
        </Border>

    </Grid>
</UserControl>
